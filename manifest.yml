---
applications:
- name: mean-stack
  random-route: true
  memory: 256M
  services:
  - mean-starter-mongodb
  env:
    NODE_ENV: cloud-foundry
declared-services:
  mean-starter-mongodb:
    label: databases-for-mongodb
    plan: standard

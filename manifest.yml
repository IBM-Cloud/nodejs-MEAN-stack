---
applications:
- name: mean
#TODO Remove this comment prior to publishing
#  host: mean-${random-word}
  memory: 128M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100


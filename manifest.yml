---
applications:
- name: mean-stack
  host: mean-${random-word}
  memory: 256M
  services:
  - mongodb
  env:
    NODE_ENV: cloud-foundry
declared-services:
  mongodb:
    label: compose-for-mongodb
    plan: Standard
